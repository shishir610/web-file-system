const initialState = [
    'root'
]

const currentpath = (state = initialState, action) => {
    const { type, payload } = action;

    // switch (type) {

    // }
    return initialState
}

export default currentpath